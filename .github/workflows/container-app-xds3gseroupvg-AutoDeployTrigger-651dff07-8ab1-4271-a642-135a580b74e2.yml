name: Trigger auto deployment for container-app-xds3gseroupvg

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-xds3gseroupvg-AutoDeployTrigger-651dff07-8ab1-4271-a642-135a580b74e2.yml'

  # Allow mannually trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPXDS3GSEROUPVG_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPXDS3GSEROUPVG_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPXDS3GSEROUPVG_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryxds3gseroupvg.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPXDS3GSEROUPVG_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPXDS3GSEROUPVG_REGISTRY_PASSWORD }}
          containerAppName: container-app-xds3gseroupvg
          resourceGroup: samplecontainerapp8ebd
          imageToBuild: containerregistryxds3gseroupvg.azurecr.io/container-app-xds3gseroupvg:${{ github.sha }}
          dockerfilePath: Dockerfile


